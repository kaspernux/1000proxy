# Add these to your .env file for Postmark (Free tier: 100 emails/month)
MAIL_MAILER=postmark
POSTMARK_API_TOKEN=your-postmark-api-token
MAIL_FROM_ADDRESS=hello@yourdomain.com
MAIL_FROM_NAME="${APP_NAME}"
